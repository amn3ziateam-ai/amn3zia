# Local build config / secrets — never commit
android/local.properties

# Gradle / Android build output
android/.gradle/
android/build/
android/app/build/
android/*/build/
android/local.properties
*.iml
.idea/
.gradle/

# TDLib binaries — fetched/built by CI, not committed to source control
android/app/src/main/jniLibs/
android/app/libs/tdlib.jar
android/app/src/main/java/org/drinkless/

# Gradle wrapper jar — generated by `gradle wrapper`, not committed (binary)
android/gradle/wrapper/gradle-wrapper.jar

# OS / editor cruft
.DS_Store
Thumbs.db
